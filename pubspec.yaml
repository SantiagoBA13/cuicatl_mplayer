name: cuicatl_mplayer
description: Reproductor Cuicatl Ultimate.
publish_to: 'none'
version: 5.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  just_audio: ^0.9.36
  just_audio_background: ^0.0.1-beta.11
  on_audio_query: ^2.9.0
  permission_handler: ^11.2.0
  shared_preferences: ^2.2.2
  palette_generator: ^0.3.3
  # NUEVO: Ecualizador integrado
  equalizer_flutter: ^0.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
